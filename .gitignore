node_modules/
.env
web/server/.env
*.db
*.db-journal
dist/
*.tsbuildinfo

# Sao lưu cục bộ — chứa dữ liệu và mã băm mật khẩu thật, tuyệt đối không đưa lên kho mã
backups/
*.backup-*
*.db.backup*

# Bản desktop (Electron): tệp build và bộ cài sinh ra
desktop/build/
desktop/release/
